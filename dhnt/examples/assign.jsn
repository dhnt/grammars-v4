/**

 */

// assign.jsn
{
    main: () {
       x = b
       x *= b
       x %= (b + c)
       x, y, z = a, b, c, d
       x, y, z = a + b - c * d / f, a || b && !c
       
       async, defer = &func(x), ~func(x, y)
    }
}